# HTTP Pub Sub Server
A simple webhook notification system that allows pushing of message to subscribers via HTTP

# Installation
```sh
   Clone the repository
   Run composer install
   create a  .env file from the .env.example file provided
   run database migration
   
   NB: Make sure to set up all databases and use right credentials.

   Start the server with 'php artisan serve'
```
